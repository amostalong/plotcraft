{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "PlotCraft default capabilities (commit 1 起步：core + dialog)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
